{
  "name": "pi-ocp-dev",
  "version": "0.1.0",
  "description": "pi coding agent tools for OpenShift developers: Prow CI monitoring, must-gather diagnostics, PR review responder, CI triage, Jira solver, and pre-commit automation",
  "keywords": ["pi-package"],
  "license": "Apache-2.0",
  "pi": {
    "extensions": ["./extensions/index.ts"],
    "skills": ["./skills"]
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
